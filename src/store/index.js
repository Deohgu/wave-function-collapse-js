import Vue from "vue";
import Vuex from "vuex";

import identities from "./identities";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    identities,
  },
});
