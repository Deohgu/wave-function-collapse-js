// CURRENT:
// Define a way to traverse the grid and check based on the rules

export default (arr) => {
  // arr.forEach((curr) => {
  //   console.log(curr);
  // });

  console.log("gridTraverve, arr: ", arr);
};
